{
  "name": "backend",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "nodemon --watch /app --ext js --legacy-watch index.js",
    "start": "node index.js"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "pg": "^8.20.0"
  }
}
